> doc待定中...

# props

- `pclass` - 小程序需父组件调整"options.styleIsolation"方可使用"pclass"prop;

  ```js
  export default {
      options: {
          styleIsolation: 'shared' //示列
      }
  }
  ```

  

# 自定义指令

